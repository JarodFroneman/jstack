# Tenant document service

Small C# service-domain fixture used by the Tier-1 Proof Plane.

Requires the .NET 8 SDK. Run the deterministic public suite with:

```text
dotnet run --project tests/TenantDocuments.Tests.csproj --configuration Release
```

The project has no NuGet package dependencies and is covered by the
[MIT licence](../../LICENSE).
