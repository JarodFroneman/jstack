# Signed webhook verifier

Small Python API-security fixture used by the Tier-1 Proof Plane.

Requires Python 3.9 or newer. Run the deterministic public suite with:

```text
python3 -m unittest discover -s tests -v
```

The project uses only the Python standard library and is covered by the
[MIT licence](../../LICENSE).
