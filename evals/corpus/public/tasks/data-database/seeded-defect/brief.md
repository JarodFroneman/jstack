# Archived document tenant scope

The document listing endpoint can optionally include archived rows. Every
result must belong to the authenticated tenant in both the default and
include-archived modes. Preserve stable ordering and archived-row semantics.
Keep the change within the repository query and return focused SQLite
behaviour evidence.
