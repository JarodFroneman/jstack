# Exact legacy configuration keys

The line-oriented configuration reader can return the value of a longer key
whose name starts with the requested key, for example treating `PORTABLE` as
`PORT`. Match complete key names immediately followed by `=`, while preserving
comments, line ordering, and ordinary lookups. Keep the change within the C
reader and return characterization-test evidence.
