target-version = "py39"

[lint]
# Keep the repository's required static pass focused on rules that identify
# invalid or unsafe Python rather than rewriting the intentionally frozen
# corpus and compatibility fixtures for style.
select = ["E9", "F63", "F7", "F82"]
